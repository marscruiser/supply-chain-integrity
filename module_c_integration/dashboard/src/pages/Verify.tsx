import React from 'react';
export default function Verify() {
    return (
        <div className="page">
            <h1>Verify Shipment</h1>
            <p>Upload X-ray scan → Compare to origin → PASS/FAIL verdict — implementation pending.</p>
            {/* TODO: Dropzone upload, shipment code input, result display with SSIM heatmap */}
        </div>
    );
}
